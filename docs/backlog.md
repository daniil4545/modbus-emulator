# Backlog — modbus-emulator

## Выполнено

- [x] v1-v2: разбор конфига, кодирование значений, серверы TCP / RTU-over-TCP / serial, PTY, симуляция
- [x] v3: разворот `count` из `template.yaml`, `byte_order`, справочник полей
- [x] M5: ручное тестирование связки эмулятор + драйвер
- [x] v1.0.0: исправление дефектов, снос обходных решений, тесты, документация (см. CHANGELOG)

## Возможные направления

Проект законсервирован — задач в работе нет. Если понадобится развитие:

- [ ] Несколько `slave_id` на одном транспорте (эмуляция RTU-шины на одной PTY-паре).
      Сейчас один сервер на устройство, поэтому два устройства не могут делить `ip:port`
- [ ] Изменение значений регистров на лету, без перезапуска
- [ ] Профили отказов: таймауты, обрывы, битые CRC — для проверки реакции драйвера
